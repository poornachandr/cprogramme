#include<stdio.h>
#include<conio.h>
void main()
{ 
 char ch;
 printf(/n"Enter a character");
 scanf("%c",&ch);
 if((ch>=65&&ch<=90)||(ch>=97&&ch<=122))
 {
  prinf(/n"character is alphabet");
  }
  else
  {
   printf(/n"character is not an alphabet");
   }
   getch;
  }; 
